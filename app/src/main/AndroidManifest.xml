<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.brindysoft.simpleslideshow">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application android:allowBackup="true" android:label="@string/app_name"
        android:icon="@mipmap/simple_slideshow" android:theme="@style/AppTheme">

        <activity android:name=".SlideshowActivity" android:screenOrientation="landscape">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity android:name=".EditPicturesActivity" android:screenOrientation="landscape" />

    </application>

</manifest>
